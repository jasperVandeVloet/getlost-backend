{
  "routes": [
    {
      "method": "GET",
      "path": "/walks",
      "handler": "walk.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/walks/count",
      "handler": "walk.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/walks/:slug",
      "handler": "walk.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/walks",
      "handler": "walk.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/walks/:id",
      "handler": "walk.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/walks/:id",
      "handler": "walk.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
